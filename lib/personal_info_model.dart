class PersonalInfoModel {
  PersonalInfoModel({
    this.idCard,
    this.firstName,
    this.lastName,
    this.address,
    this.filterAddress,
    this.birthday,
    this.ocrBackLaser,
  });

  String? idCard, firstName, lastName, address, filterAddress, birthday, ocrBackLaser;
}
